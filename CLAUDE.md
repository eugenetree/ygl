## Agent skills

### Issue tracker

Issues live in GitHub Issues (`eugenetree/ygl`). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: one `CONTEXT.md` at the repo root, ADRs in `docs/adr/`. See `docs/agents/domain.md`.
